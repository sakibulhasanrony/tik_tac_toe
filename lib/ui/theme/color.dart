//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = Color(0xFF424242);
  static Color secondaryColor = Color(0xFFFFFFFF);
  static Color accentColor = Color(0xFF18FFFF);
}
